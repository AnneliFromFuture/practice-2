@model IEnumerable<MorePractice.ServiceDoctor>

@{
    ViewBag.Title = "Список связей";
}

<h2>Список связей</h2>

<p>
    @Html.ActionLink("Создать новую связь", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Услуга")
        </th>
        <th>
            @Html.DisplayName("Фамилия доктора")
        </th>
        <th>
            @Html.DisplayName("Имя доктора")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
     {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Service.PetService)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Doctor.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Doctor.Name)
            </td>
            <td>
                @Html.ActionLink("Редактировать", "Edit", new { id = item.ID }) |
                @Html.ActionLink("Детали", "Details", new { id = item.ID }) |
                @Html.ActionLink("Удалить", "Delete", new { id = item.ID })
            </td>
        </tr>
     }

</table>
